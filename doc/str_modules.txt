Текущая структура модулей

temp 
    user 
        players
        xprint
    menu
        xprint
    game
        players
        saves
        utils
        xprint
    xstat
        players  
    players
        from configparser import ConfigParser
        from pathlib import Path
        from sys import path
    utils
        from itertools import compress
    xprint
        from shutil import get_terminal_size
    saves
        from pathlib import Path
        from sys import path
    litlib
        
temp (точка входа) - будет переименовано
user (получаем пользователя, авторизация)
players (обрабатываем данные из файла players.ini)
xprint (вывод в консоль в форматированном виде)
menu (???)
game (игровой процесс, обновление файлов)
saves (обрабатываем данные из файла saves.ttt)
utils (проверка победы, ничьи, победных комбинаций)
xstat (формирование статистических данных)
litlib (литералы для взаимодействия с пользователем)